<%= form_with(model: number) do |form| %>
  <% if number.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(number.errors.count, "error") %> prohibited this number from being saved:</h2>

      <ul>
        <% number.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="mb-3">
    <%= form.text_field :name, class: "form-control", placeholder: "Name"%>
  </div>

  <div class="mb-3">
    <%= form.text_field :phone1, class: "form-control", placeholder: "Phone Number 1" %>
  </div>

  <div class="mb-3">
    <%= form.text_field :phone2, class: "form-control", placeholder: "Phone Number 2" %>
  </div>

  <div class="mb-3">
    <%= form.text_field :phone3, class: "form-control", placeholder: "Phone Number 3" %>
  </div>

  <br/>

  <div>
    <%= form.submit class:"btn btn-success" %>
  </div>
<% end %>
